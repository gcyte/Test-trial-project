import React from 'react';
import { banner } from "../assets";

const Hero = () => {
    return ( 
        <div className='h-screen bg-center bg-no-repeat bg-cover mt-[-100px]' style={{ backgroundImage: `url(${banner})` }}>
           
        </div>
     );
}
 
export default Hero;
